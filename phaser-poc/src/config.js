// Shared constants used across scenes. Mirrors the canvas size and ball
// radius from the original vanilla-JS game (js/main.js: W, H, BR) so level
// data ported from js/levels.js can be reused without rescaling.
export const WIDTH = 960;
export const HEIGHT = 600;
export const MARGIN = 28; // playable-area border, matches physics.js's M
export const BALL_RADIUS = 11;
